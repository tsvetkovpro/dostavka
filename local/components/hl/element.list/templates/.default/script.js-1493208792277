$(document).ready(function() {
	$(".divp").on("click", ".block-on", function(event) {
		$(".block-on.active").removeClass('active');
		$(".descript.active").removeClass('active');
		$(this).addClass('active');
		$("#desc-"+$(this).attr("id")).addClass('active');
	})
});